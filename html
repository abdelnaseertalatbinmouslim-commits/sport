<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>منصة كلية علوم الرياضة - جامعة أسيوط</title>
    <style>
        /* إعدادات الخطوط والتصميم العام */
        @import url('https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;700;900&display=swap');

        :root {
            --primary-bg: #0f172a;
            --accent-color: #38bdf8;
            --glass-bg: rgba(255, 255, 255, 0.05);
            --glass-border: rgba(255, 255, 255, 0.1);
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Cairo', sans-serif;
            background: radial-gradient(circle at top left, #1e293b, #0f172a);
            color: #fff;
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* خلفية متحركة سحرية */
        .bg-animate {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            z-index: -1;
            background: linear-gradient(45deg, #0f172a 25%, #1e1b4b 50%, #0f172a 75%);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        header {
            text-align: center;
            padding: 60px 20px;
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--glass-border);
        }

        .college-title {
            font-size: 3rem;
            font-weight: 900;
            background: linear-gradient(to right, #38bdf8, #818cf8);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 10px;
        }

        .dept-title {
            font-size: 1.5rem;
            color: #94a3b8;
            letter-spacing: 1px;
        }

        /* شبكة الأقسام الاحترافية */
        .container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            padding: 50px 10% ;
        }

        .card {
            background: var(--glass-bg);
            border: 1px solid var(--glass-border);
            padding: 40px;
            border-radius: 24px;
            text-align: center;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            backdrop-filter: blur(15px);
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .card:hover {
            transform: translateY(-15px);
            border-color: var(--accent-color);
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        }

        .card h3 {
            font-size: 1.8rem;
            margin-bottom: 15px;
            color: var(--accent-color);
        }

        .card p {
            font-size: 0.95rem;
            color: #cbd5e1;
            line-height: 1.6;
        }

        footer {
            text-align: center;
            padding: 40px;
            font-size: 1.1rem;
            color: #64748b;
        }

        .signature {
            font-weight: bold;
            color: #f43f5e;
        }
    </style>
</head>
<body>

<div class="bg-animate"></div>

<header>
    <h1 class="college-title">كلية علوم الرياضة - جامعة أسيوط</h1>
    <p class="dept-title">قسم المناهج والتدريس للتربية الرياضية</p>
</header>

<div class="container">
    <div class="card">
        <h3>المحاضرات</h3>
        <p>بوابة الوصول الرقمي للمحتوى الأكاديمي التفاعلي.</p>
    </div>
    <div class="card">
        <h3>الفيديوهات التعليمية</h3>
        <p>مكتبة مرئية تشرح المهارات الرياضية والمناهج العملية.</p>
    </div>
    <div class="card">
        <h3>سيرة الأساتذة</h3>
        <p>تعرف على القامات العلمية القائمة على تدريس المقررات.</p>
    </div>
    <div class="card">
        <h3>الجداول الدراسية</h3>
        <p>التنظيم الزمني للمحاضرات والتدريبات العملية بالقسم.</p>
    </div>
</div>

<footer>
    تم التطوير بواسطة: <span class="signature">Eng. Abdel Nasser</span>
</footer>

</body>
</html>
